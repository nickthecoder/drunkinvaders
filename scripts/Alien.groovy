// Implemented by AlienShip and Shield.
// Used by Bullet to find all of the AlienShips and Shields (so that it can test for collisions).
//
// See also Human, which is a similar interface used by AlienBullets.

interface Alien {
    void hit()
}
